{
  "name": "community_tele_bot",
  "version": "1.0.0",
  "description": "eos vn community bot to check and verify RAM of telegram user then determine their permissions.",
  "main": "index.js",
  "scripts": {
    "start": "node dist/src/index.js",
    "build": "tsc && npm run copy:assets",
    "dev": "nodemon --exec ts-node src/index.ts",
    "copy:assets": "cp -rf src/assets dist/src/assets"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@wharfkit/antelope": "^1.0.8",
    "@wharfkit/contract": "^1.1.5",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/node-cron": "^3.0.11"
  }
}